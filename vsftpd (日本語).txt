# 設定ファイルの例 /etc/vsftpd/vsftpd.conf
#
# デフォルトのコンパイル済み設定は、かなり偏執的です。このサンプルファイル
# ftpデーモンをより使いやすくするために, # 少し緩和しています.
このサンプルファイルでは, ftpデーモンがより使いやすくなるように, # 少し設定を緩めています.
#
# これを読んでください：このサンプルファイルはvsftpdのオプションを網羅したものではありません.
# vsftpd.conf.5のマニュアルページをお読みください.
# 機能については, vsftpd.conf.5のマニュアルページを読んでください.
#
# 匿名FTPを許可するか? (注意: これをコメントアウトすると, デフォルトで許可されます).
anonymous_enable=NO
#

ローカルユーザのログインを許可する場合, # これをコメントアウトしない.
# SELinux がSEのチェックを強制している場合 bool ftp_home_dir
local_enable=YES
#
# これをアンコメントすると、あらゆる形式のFTP書き込みコマンドが有効に # なります.
write_enable=YES
#
# ローカルユーザーのデフォルトのumaskは077です.これを022に変更したいかもしれません、
# (022は他のほとんどのftpdで使用されています)。
local_umask=022
#
匿名FTPユーザがファイルをアップロードできるようにするには, # この設定を解除してください.これは
これは, # 上記のグローバル書き込み許可が有効になっている場合にのみ有効です.また, # FTPユーザーが書き込み可能なディレクトリを作成する必要があります.
# FTPユーザーが書き込み可能なディレクトリを作成する必要があります.
# SELinuxがSEのチェックを強制している場合 bool allow_ftpd_anon_write, allow_ftpd_full_access
#anon_upload_enable=YES
#
匿名FTPユーザーに新しいディレクトリを作成させたい場合は、 # このコメントを解除してください。
# 新しいディレクトリを作成できるようにしたい場合は、これをアンコメント # してください。
#anon_mkdir_write_enable=YES # 匿名FTPユーザが新しいディレクトリを作成できるようにしたい場合は, # このコメントを解除してください.
#
# ディレクトリメッセージを有効化します.
# ディレクトリメッセージを有効にする.
dirmessage_enable=YES
#
# アップロード/ダウンロードのロギングを有効にする.
xferlog_enable=YES
#
# PORT転送の接続がポート20(ftp-data)から行われるようにする.
connect_from_port_20=YES
#
# 必要であれば、アップロードされた匿名ファイルの所有者を
# 必要であれば, アップロードされた匿名ファイルを別のユーザが所有するように # 設定することができます.注意！アップロードされたファイルに "root "を使うことは # 推奨されません!
# 推奨されません！
#chown_uploads=YES
#chown_username=whoever
#
# ログファイルの保存先を上書きすることもできます。デフォルトは以下の通り。
# デフォルトは以下の通りです。
#xferlog_file=/var/log/xferlog
#
# 必要であれば, ftpd標準のxferlog形式でログファイルを作成することも # できます.
この場合, # デフォルトのログファイルの場所は/var/log/xferlogであることに注意してください.
xferlog_std_format=YES
#
# アイドルセッションのタイムアウトのデフォルト値を変更することができます。
#idle_session_timeout=600
#
# データ接続のタイムアウトのデフォルト値を変更しても構いません.
#data_connection_timeout=120
#
# ftpサーバーが完全に隔離された, かつ利用できないユーザとして利用できる # 一意のユーザをシステム上に定義することを推奨します.
# ftpサーバが完全に分離された非特権ユーザとして利用できる一意のユーザを # 定義しておくことを推奨します.
#nopriv_user=ftpsecure
#
# これを有効にすると, サーバは非同期のABORリクエストを認識します.セキュリティ上推奨しません。
# セキュリティ上推奨されません(コードは自明ではありません)。有効にしないと、
有効にしないと, # 古いFTPクライアントを混乱させる可能性があります.
#async_abor_enable=YES
#
# デフォルトでは, サーバはASCIIモードを許可するふりをしますが, 実際には # リクエストを無視します.
# リクエストを無視します.以下のオプションを有効にすると, # サーバは実際にASCIIモードを実行します.
# ASCIIモードのときにファイルのマングリングを行うようにするには、 # 以下のオプションを有効にします。vsftpd.conf(5)のマニュアルページには、以下の説明があります。
これらのオプションを無効にした場合の動作については, # vsftpd.conf(5)のマニュアルページを参照してください.
一部のFTPサーバでは, ASCIIのサポートによってサービス拒否攻撃(DoS)が # 許されることがあるので注意してください.
vsftpdはこの攻撃を予測しており、常にこのような # 攻撃を受けていました。
は, この攻撃を予測し, # 生ファイルのサイズを報告することで # 常に安全な状態を保ってきました.
# 生ファイルのサイズを報告する.
# ASCII manglingはプロトコルの恐ろしい機能である。
#ascii_upload_enable=YES
#ascii_download_enable=YES
#
# ログインバナーの文字列を完全にカスタマイズすることができます：
#ftpd_banner=Welcome to blah FTP service.
#
# 許可されない匿名メールアドレスのファイルを指定することができます.どうやら.
# ある種のDoS攻撃対策に有効です.
#deny_email_enable=YES # (デフォルトは以下の通り)
# (デフォルトは以下の通り)
#banned_email_file=/etc/vsftpd/banned_emailsを指定する。
#
ローカルユーザのリストを明示的に指定すると、そのユーザのホーム # ディレクトリに chroot() することができます。
# ディレクトリに chroot() します。chroot_local_userがYESの場合、このリストは以下の # ユーザのリストになります。
# chroot()しないユーザーのリストになります。
# (警告! chrootは非常に危険です。chrootを使用する場合、次のことを確認してください。
# chrootを使用する場合は、そのユーザがchroot内のトップレベルディレクトリに # 書き込み権限を持っていないことを確認してください。
# (警告! chrootは非常に危険です。）
#chroot_local_user=YES
#chroot_list_enable=YES
# (デフォルトは以下の通り)
chroot_list_file=/etc/vsftpd/chroot_list # (デフォルトは以下のとおり)
#
# 組み込みの ls の "-R" オプションを有効にすることができます。デフォルトでは無効になっています。
# デフォルトでは無効になっています.
# デフォルトでは無効になっています.しかし, "ncftp "や "mirror "などの一部の壊れたFTPクライアントは # "-R "オプションの存在を前提としています.
# そのため、これを有効にする強力なケースがある。
#ls_recurse_enable=YES
#
listen "ディレクティブが有効な場合、vsftpdはスタンドアロンモードで動作し、 # IPv4ソケットをリッスンします。
# IPv4ソケットを待ち受けます。このディレクティブは
# listen_ipv6ディレクティブと併用することはできません.
listen=NO
#
# このディレクティブはIPv6ソケットでの待ち受けを有効にします.デフォルトでは
デフォルトでは, IPv6の "any "アドレス(::)でIPv6とIPv4の両方のクライアントからの # コネクションを受け付けます.
# とIPv4クライアントの両方からの接続を受け付けます.IPv4とIPv6の両方で待ち受ける必要はありません.
# ソケットでリッスンする必要はありません.IPv4とIPv6の両方の # ソケットをリッスンする必要はありません.
(特定のアドレスをリッスンしたい場合など), vsftpdのコピーを2つ # 用意して2つの設定ファイルを実行する必要があります.
# ファイルを用意する必要があります.
listen_ipv6=YES # とコメントされていることを確認してください！
listen_ipv6=YES

pam_service_name=vsftpd
userlist_enable=YES
tcp_wrappers=YES。
